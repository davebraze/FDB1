##' @details Read a 'merge' file (*.mer), as might be exported from Filemaker.
##'
##' This is a simple wrapper around \code{\link{read.csv}} to simplify reading tables exported from
##' Filemaker as 'merge' files.
##' @title Read a 'merge' file (*.mer), as might be exported from filemaker.
##' @param filename A *.mer file, as generated by filemaker.
##' @param columns Columns to select from *.mer file.
##' @param ... Additional arguments passed to \code{\link{read.csv}}.
##' @return A data.frame
##' @author Jon Gordils
##' @author David Braze \email{davebraze@@gmail.com}
##' @seealso
##' \code{\link{read.csv}}
##' \code{\link{read.table}}
##' @export
read.mer <- function(filename, columns, ...){
    readFile <- read.csv(filename, ...)
    ## Check to ensure that all requested columns are in the *.mer file. If not, then stop.
    if(!all(columns %in% colnames(readFile))){
        stop(paste(columns[!(columns %in% colnames(readFile))], "not in", filename, sep = " ", collapse = " & "))
    }
    merDF <- readFile[columns]
    return(merDF)
}
